# Pry.config.editor = "mate"
# if defined?(Rails)
#   self.send(:include, Rails::ConsoleMethods)
#   me = User.where(email: 'phil@philmonroe.com').first if defined?(User)
#   Rails.logger = ActiveRecord::Base.logger
# end